__pycache__/
*.pyc
.DS_Store
# out 目录内容整体忽略（模型权重文件），但保留模型演进族谱文档
# 注意：必须用 out/* 而非 out，否则 Git 不会进入目录，排除规则 ! 无法生效
out/*
!out/MODEL_LINEAGE.md
website/
docs-minimind/

# ===== 本地实验产物（不入库）=====
# Python 虚拟环境
.venv/
venv/

# 训练检查点（含优化器状态，单个 ~300MB）
checkpoints/

# 下载的数据集与切分的子集
dataset/*.jsonl

# 导出的模型格式
gguf/
scripts/minimind-*/
model_learn_tokenizer/

# 本地下载的 Reward Model
reward_model/

# 运行日志
*.log
